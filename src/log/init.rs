// Stub — will be implemented in a later wave.

use crate::log::builder::LogBuilder;
use crate::log::error::{InitError, InitGuard};

pub(crate) fn install_with_config(_builder: LogBuilder) -> Result<InitGuard, InitError> {
    unimplemented!("install_with_config is not yet implemented")
}
